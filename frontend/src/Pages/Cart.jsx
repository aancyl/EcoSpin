import React from "react";
import CartItems from '../Components/CartItems/CartItems';

const Cart = () => {
    return (
        <div>
            <CartItems />
            {/* Additional checkout or proceed buttons can be added here */}
        </div>
    );
}

export default Cart;